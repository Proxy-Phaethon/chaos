//! Library entry point for Chaos.
//!
//! The binary in `main.rs` wires these packages into a CLI, while the
//! packages themselves remain reusable library modules.

pub mod engine;
pub mod generator;
pub mod manifest;
