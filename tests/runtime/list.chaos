register:
    state: integer = 1,
    state: fruits, list = {'apple', 'banana', 'blueberry'};

logic {integer > 0};
    list fruits
        (push 'strawberry')
        (push 'raspberry')
        (pop),

execute