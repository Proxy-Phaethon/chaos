register ('experiment'):

    state: x = 3,
    state: name = 'Zia',
    state: list_one, queue = {'item 1', 'item 2'},
    state: numbers, list = {'1', '2', '3'};

logic {x > 0};

    constant: x < y;

    state: list_one (push 'item 3'),
    transition ('none');

execute